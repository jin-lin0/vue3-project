---
group: 题源与刷题
order: 1
---

# 牛客面试题库

## 一、JavaScript / TypeScript 基础

### 数据类型与基础概念

- JS 有哪些基本数据类型？基本类型与引用类型的区别、存储方式？开发中引用类型和基本类型各有什么需要注意的？
- null 与 undefined 的区别？
- 如何准确判断一个变量的数据类型？typeof / instanceof / Object.prototype.toString.call 的原理与区别？如何判断一个值是不是数组、是不是空对象？
- let / const / var 的区别？变量提升（函数提升与变量提升的不同）？暂时性死区（TDZ）？为什么需要块级作用域？
- 为什么 var 声明的变量可以重复声明（不报错），var 声明的变量一定是全局的吗（函数作用域内的 var 与全局 var 的区别、变量提升如何造成重复声明不报错）？
- == 与 === 的区别？
- Symbol 是什么？一般使用场景？
- 0.1 + 0.2 !== 0.3 的原因？精度丢失怎么解决（BigInt / 转字符串 / 第三方库）？
- 可变数据与不可变数据有什么区别？React 中为什么要用 set 方法改数据？引用类型传参要注意什么？
- 数组的 map() 和 forEach() 有什么区别？forEach() 能改变数组吗（区分基本类型值 / 引用类型整体 / 引用类型内部属性三种情况）？
- 数组的 slice 与 splice 有什么区别？
- 数组的 pop、push、shift、unshift、split、join 各自的作用是什么（含数组与字符串之间的转换：join 把数组拼成字符串、split 把字符串拆成数组）？
- 函数式编程思想？React 中为什么强调不可变数据？
- JS 的面向对象与 Java 的面向对象有什么不同？为什么 JS 采用基于原型的 OOP？面向对象编程相关？
- 手写单例模式（含双检查锁）？手写函数柯里化 / 实现 add(1)(2) 累加？

### 闭包 / 原型 / this

- 什么是闭包？闭包的作用和缺点是什么？应用场景？闭包导致的内存泄漏怎么定位？
- 闭包里被引用的外部变量，内存存放在栈上还是堆上？如果语言没有闭包机制，如何手动模拟闭包（让返回的函数仍能访问原局部变量）？
- 讲讲原型链？prototype 与 **proto** 的区别？从对象取属性的过程、原型链顶端是什么、如何判定属性在自身还是原型链上？
- 如何实现继承（class extends / Object.create / 组合寄生式）？构造函数和普通函数的区别？new 调用时发生了什么、函数如何知道自己被 new 调用？class 如何实现多继承（手撕）？
- 如何确定函数中 this 的指向？call / apply / bind 的区别与手写实现？箭头函数的 this 有什么特殊之处、为什么没有 arguments？严格模式下 this 的输出有什么变化？
- this 指向问题（给出代码判断输出）？读代码题：var / this / 事件循环 / for 循环的 i 值？
- 如何自己设计一个"类似 Babel 的语法机制"（语法解析、执行机制、底层实现）？

### 异步与事件循环

- 讲讲 JavaScript 的事件循环机制？宏任务和微任务的区别？各自有哪些（async/await 是否全部是微任务、messageChannel 是宏还是微）？给出一段代码问输出顺序？
- 事件循环在没有任务时是否会持续占用 CPU（浏览器空闲调度机制）？浏览器事件从物理硬件 → 操作系统 → 浏览器进程 → JS 事件回调执行的完整链路？
- 浏览器事件循环中的"帧"概念、生命周期与阶段？requestAnimationFrame 的执行时机？requestIdleCallback 有听过吗？
- JS 是单线程的，如果要处理比较重的后台任务或网络请求，它是怎么处理的？JS 引擎事件循环与 Node.js 事件循环的区别？Node.js 是单线程还是多线程？
- setTimeout 一定在给定时间之后执行吗？浏览器计时器适合用什么数据结构（最小堆）？
- Promise 解决了什么问题？Promise 状态含义与流转？Promise 链式调用原理、then 在不同情况的返回值？Promise.then 第二个参数的作用？Promise.then().catch() 写法的收益？
- Promise 常用的静态方法（all / race / allSettled / any）区别与使用场景？Promise 相关手写题（手写 Promise 核心逻辑 / Promise.resolve）？
- async / await 执行顺序？await 怎么捕获异常、try catch 能否捕获 promise 异常？async/await 与 Promise 的关系与取舍？写代码时怎么判断该用哪个？三个互不依赖的请求用 async/await 与 Promise 的区别？
- Generator 是什么？
- JavaScript 中有哪些异步方法（除了 Promise）？想并发请求接口有什么办法？
- 给出代码判断 apply / bind 的输出（代码题）？Promise 输出顺序题 / 同步异步输出题（长代码）？

### 内存 / 作用域 / 模块

- 作用域链？词法作用域与动态作用域的区别？
- 什么是执行上下文（Execution Context）？它包含哪些组成部分（变量对象 / 作用域链 / this 绑定），函数调用时执行上下文如何创建与销毁、与词法作用域的关系？
- JS 垃圾回收机制（标记清除 / 引用计数）？引用计数的缺点与循环依赖问题？哪些情况会造成内存泄漏（全局变量、定时器、闭包、DOM 引用）？
- JS 的内存管理（新生代 / 老年代）？V8 的 GC 回收？Node.js 的 V8 GC（新生代 / 老生代）？
- 模块化方案（ESM / CommonJS 等）及区别？ES Module 与 CommonJS 的核心差异、为什么 Vite 默认用 ESM？模块循环引用问题与解决？
- 全局异常捕获的方法？image 标签加载异常（后端 url 不准）如何捕获？
- 如何用浏览器原生方法实现订阅发布机制？如何拿到用户在某个页面的停留时间？如何监听哈希模式路由 # 之后部分的变化？

### 计算机基础（JS 视角延伸）

- 数组和链表的区别？JS 数组底层是数组还是链表？
- 进程和线程的定义及区别？是否了解协程？多线程竞争状态的产生原因与解决（互斥锁）？
- 已经有进程了，为什么还需要线程？进程创建和上下文切换为什么比线程更重（资源开销、地址空间复制）？
- 并发（concurrency）和并行（parallelism）有什么区别？多个任务看起来同时运行时，CPU 是否真的在并行执行（单核时间片轮转 vs 多核真正同时）？
- 举一个适合用进程隔离的场景，再举一个适合用多线程的场景（进程间隔离安全性 vs 线程间共享内存通信成本）？
- CPU 和 GPU 的区别，各自擅长什么任务？
- Node.js 的 JS 引擎是什么（V8）？Node.js 相较其他后端的优势？
- JS 与 C/C++ 的区别？指针与引用的区别？从写代码到程序运行经过哪些环节？静态链接是什么？
- 事件委托（事件代理）的理解、实现原理、优缺点？

### 函数式与 TS

- 防抖（debounce）和节流（throttle）的区别与手写实现（含在防抖里如何终止未执行的函数）？
- TS 常用工具类型 Record / Pick / Omit / Partial / Required / Readonly 分别有什么作用？
- TS 泛型介绍？keyof 等类型工具？TS 到最终 JS 的流程、Babel 原理？
- interface 与 type 的区别？何时用 type 何时用 interface？TS 可选与必选？如何为早期库（如 jQuery）补充类型？如何为动态 runtime 注入类型做静态检查？
- TS enum 编译成 JS 是什么（对象，key 和 value 都做索引）？
- TS 装饰器？
- TypeScript 如何绕过代码检查 / 类型断言？TS 中如何监听变量 / 属性的变化？
- TS 与 Vue3 的结合（用 Vue3 为什么可以不用 TS）？

---

## 二、框架原理（React / Vue）

### React

- React 和 Vue 的区别？各自适合什么场景？React 和 Vue 在 Diff 算法上的核心区别？React 与 Vue 虚拟 DOM 的区别？React 与 Vue 双向绑定的区别？Vue / React 各自的优缺点？
- React 常用版本？React 18 / 19 有哪些新特性（Actions / use hook / Server Components / cache() / useTransition 等）？React17 与 React18 升级点？
- React 主要流程介绍（从 setState 到渲染）？React 为什么只更新部分 DOM？state 执行后发生了什么？反复调用 setState 是一次渲染还是多次渲染？
- 了解虚拟 DOM 吗？setState → 虚拟 DOM → 真实 DOM 的完整过程？虚拟 DOM 的优缺点、性能真的更好吗？
- React 常用 Hooks 有哪些？各自作用？useEffect 是第一参数入参和返回值是什么、执行机制与原理？useLayoutEffect 和 useEffect 的区别、与 requestAnimationFrame 的执行顺序？
- useState 可以放在条件语句里吗，为什么？useState 返回为什么是数组不是对象？
- 了解 React Fiber 吗？为什么需要 Fiber 架构（可中断渲染）？引入 fiber 架构的原因、可中断内部如何实现、何时中断节点渲染、浏览器如何验证空闲时间？React 如何实现中断？fiber 的数据结构与算法？
- 函数组件和类组件的区别？Hooks 的优缺点？为什么要有 React Hooks、设计动机？Hooks 为什么不能放在分支逻辑（if）里？依赖过多问题怎么解决（useMemo / useCallback / 拆分 Hook）？用 useMemo 实现 useCallback？
- 受控组件与非受控组件？React 父组件更新如何防止子组件不必要的更新（React.memo / shouldComponentUpdate）？React 做优化会用哪些 hooks？
- React 状态管理工具选择与实践（Redux / Zustand / Pinia）？为什么选 Redux / 不用 redux 如何实现、redux 中间件？为什么选 Pinia？
- 自己封装的 hook 介绍？有没有写过自定义 hooks？
- React Native 为什么用 React 写 UI？react-dom 了解过吗？react native 原理？H5 和 React Native 的区别？
- JSX / TSX 的编译原理（编译成 React.createElement）？
- Diff 算法的核心原则？时间复杂度怎么从 O(n³) 优化到 O(n)？key 的作用？React 如何跨层级通信？
- 组件库项目有什么比较复杂的组件、怎么实现的？自研组件库亮点难点（如何封装一个 button / ProTable / ProForm）？组件库如何升级？
- 微前端怎么做的？微前端如何做状态保持？qiankun 接入步骤 / 子应用配置打包 / 沙箱与通信？
- 在 React 里怎么接入 Vue 组件（Web Component 包装）？前端里怎么接入 AI（流式渲染、上下文传给大模型）？

### Vue

- Vue2 和 Vue3 的区别？Vue3 的响应式原理（Proxy）？Vue2 如何监听数组的 push / pop？Vue2 直接通过数组下标赋值无法更新视图的原因与解决方案？Vue2 响应式的弊端、哪些数组方法改动不会被监听？
- Proxy 与 defineProperty 的区别？Vue3 中如何避免一开始递归加响应式带来的性能问题？为什么用 Proxy，与 Vue2 defineProperty 相比解决什么问题？
- 组合式 API 与选项式 API 的区别、优势？组合式函数（composables）是什么、怎么写？组合式 API 的优缺点？hooks 与 mixin 的区别？
- ref 与 reactive 的区别？为什么 template 里响应式不用 .value？
- Vue3 生命周期？Vue2 与 Vue3 的生命周期对应？Vue3 的 Composition API setup() 里响应式丢失的原因与解决？第一次渲染触发哪几个钩子、created 与 mounted 的区别？
- v-if 与 v-show 的区别及渲染机制？visibility:hidden、display:none、opacity:0 的核心区别？
- provide / inject 的数据是否响应式？mixin、slot 的理解与使用？插槽的使用场景？
- keep-alive 的作用与原理？
- Vue 的响应式原理？双向数据绑定的底层实现？v-model 的实现原理（input / select）？
- 组件通信方式有哪些（父子组件通信、跨层级）？全局状态管理怎么做的？
- Vuex 的原理、含义和作用？为什么选 Pinia？Pinia 相比 Vuex 的优点、两者使用场景区别？Pinia 做状态管理的意义与价值？Pinia 怎么做响应式持久化、与 Vuex 在插件机制上的本质差别？vuex 迁移到 pinia 的原因？
- Vue Router 介绍？两种模式（hash / history）区别？页面权限 / 路由鉴权怎么实现？动态路由、RBAC 权限整套实现流程？
- Vue 列表渲染中 key 的作用及对应底层渲染逻辑？用数组下标作为 key 会带来哪些渲染异常（不止插入元素）？即使严格响应式规范操作，还有哪些情况可能渲染异常？
- Vue nextTick 是什么、作用？如何结合 nextTick 优化流式渲染？vue observable 了解吗？
- 为什么用 Vue3 / 为什么选 Vue？Vue 是怎么编译的？
- 微前端 / 大屏适配方案（优缺点）？
- 一个 Vue2 的 H5 项目想迁移到 Vue3 该怎么做（迁移方案、破坏性变更兼容、组合式 API 改写、依赖与构建工具升级、风险控制）？
- history 模式刷新时为什么会出现白屏 / 404？怎么解决（服务端配置 SPA fallback / historyApiFallback）？
- 用 reactive 声明了一个表单对象，想把它的字段重置回初始值，应该怎么做（为什么不能直接 `formData = initialData` 整体重新赋值来重置、Object.assign 合并初始值为什么可以）？

### 通用

- 组件库如何适配暗黑模式？CSS Module 用过吗、原理？class 冲突怎么解决？修改 antd 组件样式如何避免被覆盖？styled-components 使用与实现原理？Shadow DOM 样式隔离？Tailwind 相比普通 css 的优势、类名太多如何维护？
- less / sass 原理？webpack 中如何配置编译 less？
- 单页面路由的理解；SPA 和 MPA 的区别及优缺点？为什么只有一个 index.html 却能显示不同页面（SPA）？
- 表单有很多字段，如何实现对应字段改变只渲染部分组件（React 渲染优化）？复杂表单动态输入项 / 状态管理与校验？
- 框架和 jQuery 的区别？vue 可以用 jsx 吗？如果给多个项的列表通过操作 DOM 插入 body 不能包 div 怎么做？

---

## 三、CSS / 渲染与布局

- 浏览器页面渲染流程是什么？HTML 解析过程中遇到 script 会发生什么？输入 URL 到页面渲染的完整过程（含 DOM/CSSOM/渲染树/布局/绘制、重排重绘例子）？
- 什么是重排（reflow）和重绘（repaint）？如何减少？合成层（GPU 合成层）如何提升渲染性能？如何开启合成层？离屏渲染是什么、有什么缺点？
- 水平垂直居中有哪些方法？为什么用 transform？相对哪个元素？有没有不改动父元素的方法？浮动怎么实现居中？
- 盒模型是什么、由什么组成？box-sizing 的作用？text-indent 是否计算在盒模型内？position 默认值、各定位值与特点（relative 是否脱离文档流、absolute 与 fixed 区别、fixed 相对什么定位）？
- flex:1 对应的三个参数（grow / shrink / basis）？flex 如何垂直水平居中？Flex 布局原理、常用属性？不知道子元素数量时如何让子元素等间距 / 等宽排列？
- 三栏布局有哪些实现方式（双飞翼 / 两侧固定中间自适应）？
- BFC 是什么、特点？清除浮动 / 高度塌陷、clear 的使用？margin 合并现象（垂直合并）？
- 不用 border-radius 如何实现一个圆？如何用 CSS 做成一个三角形、并让它保持旋转？
- 行内元素与块级元素分别有哪些？display:none 与 visibility:hidden、opacity:0 的核心区别？让页面元素隐藏的几种方式？
- HTML 的 head / body？meta / link / style？link 与 @import 的区别？link 可引入哪些资源、type 做什么、能否跨域？script 跨域吗、async / defer 区别？JS 放在 HTML 前面且无异步属性会造成什么后果？JS 报错会对页面渲染产生什么影响？
- CSS 选择器有哪些、优先级？!important 优先级？BEM 命名规范的应用场景与优缺点？
- CSS 长度单位（px / em / rem / vw / vh / rpx / vmax / vmin / 百分比）？10vh 含义？rpx 用过吗？
- 移动端适配方式（媒体查询、rem、vw/vh、px/em/rem 区别、iOS 安卓差异）？移动端安全区域（刘海 / 底部小黑条）适配（env(safe-area-inset-\*) / SafeAreaView）？
- 移动端 / 前端画圆的方式？前端拖动轴 / 滑块动态显隐怎么设置？CSS 里能写脚本吗？
- 实现一个 button 渐变效果（深红到浅红）的多种方式？鼠标的拖尾效果怎么实现？
- 单行 / 多行文本溢出隐藏？
- 定义一个动画有哪几种方式（animation / transform / CSS3 动画）？轮播图过渡效果？用 CSS 实现点击方块动态设置背景色、animation 简单动画？
- 响应式布局的方式（媒体查询、rem 等）？设计封装响应式布局的组件？i18n 原理及性能优化？
- 图片格式 jpg / svg / png / webp 优缺点与适用场景？为什么 WebP 而不用 PNG/JPG？Base64 的作用？
- canvas 与 svg 的区别及各自用途？canvas 绘制点 / 线、如何给 canvas 添加点击事件、如何绘制 5 万条数据、地图绘制大量点为什么影响性能？svg 节点渲染卡顿如何优化？
- 暗黑 / 亮色主题切换怎么实现？PC 端暗黑模式怎么适配？
- 如何做移动端 / H5 适配、iOS 安卓端适配差异？如何做 SEO 规范？
- DOM 事件流动机制（捕获和冒泡）？事件冒泡和事件捕获？父元素触发的事件怎么防止传递给子元素？事件流、事件委托、addEventListener 的 capture / 冒泡、第三个参数（useCapture）？处于事件阶段？
- 大模型输出快慢对 RAF 的优劣势、如何平衡不同输出频率下 RAF 的表现？requestAnimationFrame 是什么、作用？
- 页面抖动 bug 怎么处理？平时怎么定位和修改样式问题？
- Three.js / WebGL 项目怎么做离屏渲染？还做了哪些渲染优化？切换场景时如何防止内存泄漏（材质 / 几何体 / 纹理是否需要手动释放）？
- 常用的 CSS 布局方案有哪些？Flex 和 Grid 的区别是什么、分别适合什么场景？
- CSS 场景题：顶部导航条 + 中间内容区 + 底部 footer，内容区较短时 footer 紧贴内容区、内容区较长时 footer 固定在页面底部且内容区占满剩余高度并在自身内部滚动，怎么实现（sticky footer / flex 布局）？
- 如何用 CSS 实现一个类似「AI 正在输入」的光标闪烁 / 打字中提示效果（如对话中 AI 回复前的闪烁竖线光标，或三点跳动的「正在输入」动画）？有哪些实现方式（CSS animation / 伪元素 / SVG）与注意点？
- 前端页面由哪三部分组成，各自的作用是什么（结构层 / 表示层 / 行为层，即 HTML / CSS / JavaScript 的分工与协作）？

---

## 四、网络 / 浏览器

### HTTP 与缓存

- HTTP 有哪些版本（1.0 / 1.1 / 2.0 / 3.0）？核心区别？HTTP/2 有什么特点（多路复用 / 二进制分帧 / 头部压缩 / Server Push）？HTTP/3 为什么更快（QUIC / UDP）？
- 介绍 HTTP 强缓存与协商缓存？强缓存有什么缺点为什么还需要协商缓存？cache-control 有哪些值、与 expires 优先级谁高？协商缓存字段（ETag / If-Modified-Since / Last-Modified / If-None-Match）？启发式缓存？
- HTTP 状态码有哪些（301/302/304/401/500/502/504 等）？301 与 302 使用场景与浏览器执行区别？302 和 304 的区别？
- HTTP 报文格式？请求头字段？请求报文的 content-type 有哪些常见字段？GET 与 POST 区别（含跨域上区别）？
- HTTPS / TLS 加密流程？HTTPS 与 HTTP 的区别？证书验证（CA 签名）如何防止中间人攻击？为什么用对称 + 非对称两种加密结合？浏览器提示证书无效如何处理？
- 浏览器缓存机制？浏览器本地存储一般用在哪些场景（localStorage / sessionStorage / cookie / IndexedDB）？cookie / localStorage / sessionStorage 的区别、大小限制？IndexedDB 与 localStorage 如何选型？跨域访问 localStorage 可以吗？
- DNS 解析前端会遇到什么问题？CDN 是什么、缓存查询步骤、怎么落地？Nginx 解决跨域时是什么角色？正向 / 反向代理区别、负载均衡、静态资源缓存？
- 点开淘宝网址发现是京东（被劫持），哪里出了问题？不进行劫持如何变回？VPN 原理？
- 计算机网络的几层协议（OSI 七层 / 五层）？各层作用、与开发相关的层？为什么要分这么多层？文件传输协议（FTP 等）有哪些？
- 更新代码发版之后，用户那边显示的还是旧页面，要怎么排查？（涉及哪几层缓存、如何逐层定位与解决）

### 跨域与安全

- 什么是跨域？同源策略？浏览器怎么识别是否同源？常见跨域解决方案（CORS / Nginx 反向代理 / JSONP / dev proxy）？详述 CORS 具体配置？预检请求用到什么方法（OPTIONS）？跨域是否携带 cookie？src 与 href 的区别？
- XSS 是什么、什么情况会发生、怎么防御？CSRF 是什么、怎么发生、防御办法？XSS / CSRF 怎么获取 cookie？React 原生支持转义吗？恶意注入 script 标签怎么办（CSP）？三种 XSS 如何防护？浏览器安全攻击手段与原理、如何检测脚本注入？
- 攻击者如何伪装成用户？JWT 是什么？JWT 配置了哪些字段、业内安全隐患（易被逆向解码）如何加固？为什么不用 sessionID、cookie 的安全隐患及解决？AES / RSA 区别、公私钥？单点登录（SSO）/ 小程序授权流程？
- cookie 有哪些属性？SameSite 是什么、JS 能否获取 cookie、后端想让 cookie 失效怎么实现？cookie 和 session？
- 接口幂等如何实现（前端防重、后端幂等、数据库幂等三层）？自动化脚本向输入框填 1000w 个手机号发验证码，前端如何防范 / 规避刷接口？如何防止用户连续点击造成多次请求、并发请求打断？
- 多级故障（超时、弱网、熔断、宕机）前端如何分层降级、灰度兜底？流量高峰期后端限流、前端节流、队列削峰如何联动？前后端异步链路状态一致性等级与兜底策略？
- 前端安全实践（接口加密、token 鉴权、CSP）？Prompt Injection 与越狱的区别；前端如何防止提示词注入？
- POST 请求一定会触发预检（OPTIONS）请求吗？哪些情况下 POST 仍属于简单请求、不触发预检（如 Content-Type 为 application/x-www-form-urlencoded、multipart/form-data、text/plain）？

### 实时通信与流式

- WebSocket 的完整流程和底层原理？和 SSE 的区别？心跳保活和断线重连（指数退避）怎么实现？还有哪些全双工协议？用 WebSocket 还是 SSE（为什么 AI 对话优先用 SSE 而非轮询 / WebSocket、文本流式输出选 SSE、语音转写选 WebSocket）？SSE 与 HTTP streamable 的区别？SSE 不支持 fetch 为什么不用 fetch-eventsource？
- SSE 原理？SSE 连接过程、前端如何流式响应？断连重连、指数退避、断点续传补丢失文本怎么实现？后端什么时候推送、前端断连怎么处理？大模型返回中断如何处理（断点续传 / 重连）？
- 打字机逐字输出怎么实现？后端一直吐数据如何保证前端不卡（Web Worker 释放计算线程）？流式场景下 Markdown 表格如何判断"完整解析"再一次性渲染、HTML 标签流式缓冲区处理？前端接收流式数据的方式（服务端 yield、前端增量渲染、处理中断 / 重连 / 超时）？
- 文本流式输出选用 SSE、语音转写选用 WebSocket 的原因？乐观更新如何实现？
- 用户点击"停止生成"，后端如何立即终止 LLM 推理？多轮对话 + 流式输出如何保证消息不乱序、上下文不丢失？跨服务流式透传（gRPC streaming / HTTP2 SSE）？

### 其他

- TCP 和 UDP 的区别？TCP 如何实现可靠性传输（拥塞控制、慢启动）？TCP 三次握手、四次挥手，为什么需要三次握手？最后一次 ack 丢失怎么办？TCP 粘包？游戏模块一般用什么协议？
- XMLHttpRequest（XHR）的 readyState 有哪些状态码？fetch vs XHR / Ajax 应用场景？ajax 底层原理？
- CDN 怎么做？静态资源（JS/CSS/图片）如何处理？hash 强缓存策略？CDN 与 COS 等对象存储的优势对比？
- 从输入 URL 到页面展示的完整过程（含三次握手/四次挥手、具体重排重绘例子）？浏览器事件从硬件 → 操作系统 → 浏览器进程 → JS 回调的完整链路？
- 浏览器有哪些进程？进程和线程的区别？浏览器渲染进程 / 渲染原理？
- 客户端服务器重连的策略？实时音频通话跟大模型交互，期间的时延怎么控制？
- 微信登录 / 二维码扫码登录怎么实现的？介绍流程？PC 端怎么感知登录状态并跳转？
- 大文件分片上传如何实现？秒传、断点续传（基于 HTTP Range）、进度同步？切片用的什么 API？弱网怎么办？前端如何显示上传进度？大文件拆分上传、断线重连如何实现重传？
- 音视频通话：怎么实现码率自适应调整、怎么判断网卡调整码率、交换媒体信息的安全问题？视频编码 H264 与 H265 的区别？
- 跨端开发框架（React Native / 小程序 / 公众号）了解？WebAssembly 电子签章 / 跨语言交互（与原生接口）？
- JSBridge 是什么、有了解吗？H5 / WebView 与 Native 之间如何互相通信（协议约定、调用与回调机制）？
- WebView 加载慢 / 白屏怎么优化（预加载 / 离线包 / 资源缓存 / 内核复用等）？App 新增蓝牙与硬件连接后，硬件广播的信息前端页面怎么接收与展示？

---

## 五、工程化 / 性能 / 部署

### 性能优化

- 前端性能优化有哪些手段（编译 / 网络 / 渲染性能）？首屏加载速度怎么优化？页面打开很慢可能是哪些问题？路由懒加载为什么能优化首屏、改善 LCP？为什么做预加载和骨架屏、弱网降级？
- 性能优化量化：每个优化方法分别提升了多少、为什么能提升这么多？性能优化案例（首屏加载、资源压缩、CDN 策略）？
- 出现白屏问题如何排查？前端性能指标（FCP / LCP / CLS / FPS）怎么采集？LCP 是什么、如何查看？FCP 指什么、如何测量？FP / FCP 区别？
- 懒加载策略：IntersectionObserver API 和 loading="lazy" 的区别？图片懒加载原理与手写核心逻辑（offsetTop）？表格懒加载（分页 / 按需请求）具体怎么实现（触发条件、前后端配合）？
- 极大量数据（如 10 万条）下拉加载仍卡顿如何进一步优化？虚拟列表 / 虚拟滚动的实现思路（原理、实现方式、前缀高度表 + 二分查找、合并单元格时懒加载数据结构与渲染逻辑）？瀑布流怎么做、用过虚拟列表吗？
- 频繁 append + 一次性渲染几十万行节点导致主线程阻塞、页面卡顿怎么优化（分片渲染 requestIdleCallback / DocumentFragment）？滚动触底加载导致 DOM 过多卡顿如何优化（除虚拟列表外）？长列表遍历阻塞主线程如何解决（Web Worker）？
- ECharts 中 canvas 和 SVG 区别、优缺点？ECharts 图表自适应、放大缩小卡顿 / 动画卡顿怎么处理？复杂计算阻塞主线程怎么办？多图表向下滚动白屏卡顿怎么优化？如何监控每个组件的性能数据？D3 与 ECharts 的区别？为什么选偏底层的 D3？点击热力图、请求数量限制？
- 用户反馈页面加载慢 / 白屏怎么定位？项目发布到线上后如何监控整体服务质量（埋点 SDK、性能指标 SDK 内部实现、告警推送）？服务端优化方案（CDN vs COS 存储优势）？
- 用户操作卡顿如何快速定位问题？了解控制台的 Performance 面板吗、具体怎么用它分析长任务与火焰图？
- prefetch 和 preload 分别是什么作用、有什么区别（优先级 / 加载时机 / 使用场景）？怎么验证 prefetch 到底有没有生效？LCP 的测量终点是固定的吗？

### 构建与部署

- Vite 为什么启动很快？Vite 热更新原理？为什么选 Vite？Vite 如何兼容老浏览器？Webpack 与 Vite 的区别（含 Rollup / RSpack 迁移兼容性）？
- Webpack 打包流程？loader 和 plugin 的作用？Webpack 对动态 import() 怎么拆 chunk、运行时怎么加载？哪些情况没把模块拆成独立 chunk？splitChunk 怎么配置？webpack 热更新原理？Tree-Shaking 原理、打包工具如何实现？
- 分包 / 代码分割 / 路由懒加载（React.lazy + Suspense）？代码分割是否越细越好、判断标准？小程序分包思想怎么落地？分包策略（按路由分块 + 公共依赖提取，preload 优化 LCP）？模块联邦？
- 路由懒加载 + 页面过渡动画实现思路？加载延迟怎么处理？React 组件懒加载失败如何捕获与处理（Error Boundary）？
- i18n 国际化方案（i18next + react-i18next）？语言包很大怎么优化（动态 import）？
- Nginx 部署与 HTTPS 证书配置？部署方式有哪些？测试和生产环境打包有什么侧重点？SSR / ISR / 离线包？SSR 的作用、nextjs 的水合问题？BFF 上游服务报错机制？
- 如何提取 URL 中 ? 后和 # 后的参数？如何设计一个开源 Code Editor SDK 会暴露哪些 API？
- 构建产物中如何检测重复包（duplicate packages）、如何实现依赖去重 / 版本收敛策略？首屏产物体积还能从哪些角度进一步优化？
- 前端打包部署的完整流程说一下？日常部署 / 排查会用到哪些 Linux 系统命令？
- 项目里 Next.js 是用来做什么的？它算是这个项目的后端吗（SSR / API Routes / 全栈边界怎么划分）？
- Next.js 的客户端组件（Client Components）与服务端组件（Server Components）的区别是什么、各自适用什么场景（'use client' 边界、数据获取与打包体积）？

### 包管理 / 工程化 / 架构

- 包管理器 npm / pnpm / yarn 区别？lock 文件作用？pnpm 如何避免幽灵依赖、npm 是否真的扁平化？
- Babel 的作用与原理？如何保证跨浏览器兼容、polyfill 避免全局污染？ES6 转 ES5 如何实现？编译时与运行时的区别（Webpack vs Vite）？
- Monorepo 是什么？多包管理（pnpm workspace / lerna）？为什么用多层架构、与 MVC / MVVM 区别？module / Controller / service 划分模式叫什么？依赖注入原理？
- 微前端原理（CSS / JS 沙箱 + 通信）？自动创建子应用更自动化的方式？如何说服做微前端改造？多租户前端改造、权限路由清理、iframe 集成第三方页？
- 灰度发布策略与原理？设计模式（观察者模式 / 工厂模式等）优点？你所有项目里最能体现工程化能力的是什么？
- 低代码平台功能设计、技术难点？拖拽式 Dashboard 系统设计？用 AI 还原设计稿还原度及提升？
- 技术选型标准（框架对比、工具链评估）？跨语言交互可行性（WebAssembly 与原生接口）？为什么用 electron 而不用普通 web？
- 大屏适配方案有哪些、优缺点？
- 给你一个需求，如何从 0 到 1 搭建一个项目（技术选型、脚手架与目录结构、规范约束、构建与部署链路）？为什么要对已有项目做重构？

### 安全与登录

- 双 Token（AccessToken + RefreshToken）无感登录怎么实现？RefreshToken 也过期了怎么处理？基于双 token 实现单点登录？并发请求、登录态管理、持久化、鉴权？
- 鉴权放在哪一层（网关 / 登录中台 / 业务服务）？按钮级别前端权限如何实现、页面刷新时如何恢复路由与权限信息、只靠前端是否足够（后端如何兜底）？RBAC 权限系统设计？
- XSS / CSRF 前端防御（见网络章节）。

### Git 与质量

- Git 协作：Merge 和 Rebase 的区别与使用场景？冲突解决？本地 commit 5 次未 push，回滚到第 3 次后如何找回第 4、5 次（stash / cherry-pick / amend / force push）？常用 git 命令？
- 如何保证代码质量 / 不引发线上问题（除 eslint 外、除 AI 工具外）？渐进式迁移？项目开发前有没有提前思考性能问题？
- AI 辅助与工程质量：平时如何借助 AI 工具提高效率、解决问题？如何判断和验收 AI 生成的代码是否可用、可维护、符合项目规范？用 AI 改代码时是否用单元测试 / 集成测试避免新 bug？AI 生成代码占比、如何把控质量？vibe coding / Spec Coding 流程、中途错了怎么改正？用 AI 工具辅助工程化（如 Harness / Spectacle）？
- 如何检查 / 验证 AI 生成的单元测试不是"作弊"过的（确保测试真实有效、非摆设，而非只为了跑通或凑覆盖率）？

### 文件与上传

- 大文件分片上传的哈希计算怎么做（md5）？断点续传怎么知道哪些分片已经传过？大文件上传弱网怎么办？
- 文件下载断点续传（基于 HTTP Range）怎么实现？文件上传如何显示上传进度？

### 后端 / 数据 / 高并发（前端视角）

- 为什么后端用 mongoDB 而不用 mysql（对比）？对 redis 的理解？本地数据库用的什么？IndexDB 与 Dexie 对比？
- 高并发场景：1s 内对数据库 10w 次写入如何优化？后端高并发 1w 个接口都 down 了怎么处理？
- 消息队列相关：RabbitMQ / Celery 解耦异步任务（投递链路、交换机、持久化、死信 / 延迟队列、优先级、重试）；高并发下为何弃 Redis List/Stream 选 RabbitMQ；消息丢失 / 重复消费 / 死信堆积前后端如何兜底？多消息投递结果如何与发送请求对应？
- 异地数据同步如何实现？多用户并发操作同一资源竞态冲突（前端锁 / 服务锁 / 分布式锁）？
- 如何上报埋点的 api、如何确保页面关闭前请求正确发送（beforeunload）？告警推送实现方法？
- 如果由你设计后端，数据库表结构怎么设计、表与表之间关系（一对一 / 一对多 / 多对多）如何处理？
- 有 Node.js 服务端开发经验吗？用过 Egg.js 之类的 Node 企业级框架吗、约定式目录与插件机制是怎样的？

---

## 六、AI / Agent（前端 Agent / AI 研发）

### Agent 基础

- 介绍一下 Agent 的核心组成模块（规划 / 记忆 / 工具 / 行动）？给新同事解释 Agent 四大核心模块你会怎么讲？Agent 与传统 Workflow 的核心区别？
- Agent 和普通 Chatbot 的区别？已有成熟大模型，为什么还需要智能体？Agent 能帮大模型解决哪些原生处理不了的问题？
- ReAct 的执行流程是什么？ReAct / Plan-and-Execute / Reflection 的区别？ReAct 比单纯 LLM 强在哪？实际开发中如何避免"思考与行动脱节"？Planning 是由框架还是大模型做的、有哪些方法？
- Function Calling / Tool Calling 的完整流程？Agent 的自主决策、工具调用链路怎么设计？什么场景下建议用多 Agent 而不是一个全能 Agent？
- 如何设计一个 Agent（要考虑哪些模块）？Agent 的循环执行逻辑、依靠什么条件判断循环结束 / 输出结果？success / failure 状态由什么机制判定？Agent 循环是否以本轮是否调用工具作为结束判断？
- 多 Agent 系统怎么协作？常见架构（中心化 / 去中心化 / 分层）？主子 Agent 架构、子 Agent 之间上下文怎么传递？子 Agent 失败如何重试 / 降级？
- 多个 Agent 同时跑时 State 管理与 Checkpoint 机制、状态竞争怎么避免？两个 Agent 协作（如生成文案 + 审核合规）之间怎么通信（结构化消息 vs 自然语言）？如果一个 Agent 宕机或超时另一个怎么处理？
- 多模型如何动态路由（按任务 / 成本 / 延迟 / 效果选模型）？所有模型超时或故障怎么兜底、何时规则引擎、何时人工、恢复后怎么回切？能否让系统自动发现问题、尝试策略、验证效果并自动生效和回滚？
- Agent 死循环、工具循环调用如何处理？执行太长或死循环怎么解决？Agent 模式怎么实现"图片定位代码"？Agent 模式的底层实现逻辑？
- 如何控制 Agent 的「自主性」边界（严格规则引擎 vs 允许自主推理）？如何设计安全护栏（Safety Guardrail）？
- 接到一个新需求时，应该用一个普通 API / 函数调用解决，还是用 Agent（自主规划 + 工具调用）来解决？各自的适用边界与判断标准是什么（如何判断一个需求该用确定性代码 / API 还是自主 Agent）？

### 记忆与上下文

- Agent 的记忆机制有哪些（短期 / 工作 / 长期）？短期记忆具体实现方式（窗口上限、达到上限让 Agent 总结、保留最近几轮）？长期记忆怎么设计（静态 / 动态）？什么时候触发存储？如何防止记忆膨胀（去重 / 淘汰）？
- 每一轮对话如何逐步叠加到上下文？如何判断"快到上限了"、什么时候触发总结？长短期记忆的区别、短期记忆里会存储 function call 内容吗？
- 大模型怎么决定长期记忆是否需要召回？更新策略、是否遇到过记忆污染？企业级记忆中用户纠错（不一定对）怎么处理？多个窗口对话记忆性、所有对话用户画像怎么做？
- 上下文窗口 / Token 超限怎么办？上下文压缩机制怎么做（滚动摘要 / 截断阈值 / LLMLingua / Rerank / Map-Reduce）？对上下文渐进式披露的理解（Claude Code 的渐进式披露机制）？
- 上下文里筛选哪些内容、从哪获取、什么时机注入、如何保证需要的内容成功注入？除会话窗口长度、上下文压缩外，上下文还有哪些技术点？
- 多 Agent / 多异步任务场景下怎么防止上下文污染？多轮对话 / 长文本怎么处理？

### Skill 与工具调用（MCP / Skill / Function Calling）

- 你对 Skill 的理解是什么？Skill 与传统提示词工程、MCP 三者之间差异？Skill 分层体系（内置 / 用户 / 项目）？用户输入怎么和 Skill 匹配 / 路由？有没有 Skill 沉淀机制？Rules 和 Skills 的区别、为什么不把 Skills 指导写进 Rules？
- Function Calling / Tool Calling 完整流程？MCP 与 Function Calling 的区别、两者关系？MCP 与本地 tool 有什么不同？MCP 的核心工作流程？MCP 最新技术（SSE 与 HTTP streamable 区别）？MCP Server 怎么知道哪个用户调用了哪个工具？MCP 与 Skills 的区别、能否只用 skill + 脚本替代 MCP？CLI 与 MCP 区别？A2A 协议、SubAgent 是什么（MCP 是 AI 与工具连接标准，A2A 是 AI 与 AI 连接标准）？
- MCP 的传输方式（SSE / 流式）？开发一个 MCP 服务器的大概思路？MCP 返回结果支不支持流式？MCP 负载很高不堪重负怎么解决？MCP 有什么安全问题、如何解决？把某个接口封装成 MCP 工具时标准化接口怎么设计、兼容性问题？
- Skills 与大模型交互的流程？大模型怎么调用 Skills（仓库有 100 个 Skills 时上下文怎么办、意图识别 / 渐进式披露）？skills 输出结构化数据如何保障大模型输出 JSON 格式？怎么写一个 skill？
- Agent 怎么决定调哪个工具？配置了十多个工具怎么避免模型选错（工具描述模板化、每步只推荐一个工具）？工具调用的 Schema 设计核心（名字精准 / 描述说清何时调 / 参数 schema 严格）？如何减少工具过多带来的 Token 消耗？
- 大模型经常捏造工具调用参数、返回 JSON 不标准怎么解决？工具调用失败 / 超时 / 结果冲突怎么处理？模型回答不稳定怎么评估和兜底？
- 动态加载 Tool 与 Tool 注册怎么做的？未来 Tool 变多怎么优化？工具的统一调度和审计？大模型产生流式结果、结果只拿到一半怎么办（前端侧）？能否让大模型自己排除 api 超时和报错（SKILL）？
- Skill 和 Tool 有什么区别？你的 Agent 里用了 Skill 还是 Tool、分别在什么场景下用？

### RAG

- 介绍一下 RAG 的整体流程？RAG 解决了大模型的什么核心问题（幻觉 / 知识过时 / 私有数据）？为什么不能只做一次向量检索、线上系统怎么做？早期用 RAG 后来又不用了的原因？
- RAG 中如何选择 Chunk Size / Chunk Overlap？chunk 怎么切（语义段落、带多级标题索引）、大小一般多少、决定依据？PDF 中文分块如何实现？
- embedding 模型选型？如何判断向量相似度（余弦相似度 / 欧几里得距离，工程应用区别）？除了余弦还了解哪些相似度算法？
- 向量召回和 BM25 关键词召回各自适合什么场景？为什么做混合检索（全文索引 + 向量索引）？多阶段 RAG（BM25 + 向量混合）融合逻辑怎么设计？在什么环节提升效果？
- Rerank 在 RAG 链路里起什么作用、什么时候必须加？重排序怎么做？
- RAG 召回率低 / 幻觉怎么排查与治理？召回率怎么提高？为什么向量检索分数高不一定好？为什么对话直接拿用户原话去检索效果不好？RAG 与微调怎么选？
- RAG 系统如何评估、为什么不能只看最终答案？准确率多少？向量用的什么索引、数据量多大、查询延迟多少？向量检索准确性怎么评估校验？
- 知识库如何构建？一万个长文档怎么构建 RAG 知识库？知识检索如何提升回答正确率？如何解析知识库中的表格 / 图片文件？
- PDF 扫描件 / OCR / 表格结构化处理过程中踩过什么坑？识别准确率能到多少？
- RAG 引用标注和可解释性怎么实现（回答的某句话如何关联到对应文档）？前端处理 RAG 知识库引用失效与权限不足的交互降级？
- 项目中用了几个模型（embedding / 路由 / LLM / Rerank）？LLM Wiki 了解过吗？
- 为什么要先切分父子块、再做 embedding（父子分块 / small-to-big 检索带来的收益，子块召回、父块补全上下文）？

### 框架与选型

- LangChain 和 LangGraph 的区别？为什么用 LangGraph 而不是简单 LangChain？为什么选 LangChain、不直接调 OpenAI 接口？不用现成框架而自研的利弊？
- Agent 框架你用过哪些（LangChain / LangGraph / CrewAI / Coze / Dify / LlamaIndex / Eino / Agentscope 等）？各自优缺点？Coze / 扣子是 Agent 平台还是工作流平台？
- SpringAI 的核心作用和适用场景、怎么实现记忆管理、与直接用 AI SDK 的区别？AI 应用框架应该学哪一门（围绕模型、工具、记忆、编排、范式）？
- 前端 Agent 项目技术选型（为什么选 Go / TypeScript）？整个对话平台链路、Agent 分层架构（UI 层 / 模型层 / 中间层）？

### 大模型基础

- 什么是大语言模型，与传统 NLP 模型本质差异？Token 是什么？同一段文本在不同模型 Token 数可能不同、怎么计费？上下文窗口的本质、与会话记忆的关系？
- Self-Attention 的本质、计算步骤？QKV 为什么要这么分？同一个 token 向量每次一样吗？Encoder 与 Decoder 各负责什么、为什么现在大模型大部分 Decoder-only？RoPE 是什么、相比绝对位置编码优势？GQA / MHA / MQA 区别？LayerNorm 与 RMSNorm 区别、为什么要 LN？
- 采样参数 Temperature / Top-k / Top-p 怎么用？涌现能力是什么？弱 AI 与强 AI 区别？
- 什么是大模型幻觉？如何缓解（提示词 - 工具 - RAG - 微调）？降低幻觉、提升 Agent 流程稳定性？Agent 产生幻觉 / 乱调工具怎么缓解？
- 预训练 / SFT / RLHF / DPO 的区别与目的？什么场景选择微调而不是 RAG？LoRA / QLoRA 原理、一般加在哪些模块、秩取多少？GRPO 与 PPO 核心区别？RLHF 中 reward model / Actor / Critic / Reference Model 分别做什么、为什么要 KL 约束？对齐税怎么减少、微调后能力退化怎么防止？PEFT 有哪些方法？
- 分布式训练常见模型并行方式、ZeRO-1/2/3 区别？训练数据集怎么构建、学习率高低怎么判断？
- 大模型多模态？大模型 Markdown 转 HTML？长上下文模型的注意力稀释（lost in the middle）、标称上下文用到 60-70% 就开始下降？
- KV cache 原理和作用？推理时延怎么优化（Continuous batching / PagedAttention / Speculative decoding / 量化 / 蒸馏）？模型剪枝与量化原理？如何优化长文本生成显存占用？
- Prompt 是什么、可维护的 Prompt 通常包含哪些部分？Prompt 工程见解、常见结构、多阶段 Prompt 策略？Prompt Injection 与越狱区别、RAG 如何防御间接提示词注入、前端如何防止提示词注入？
- 项目中为什么选择 MLP 而不是 Transformer / LSTM（各自适用场景与模型容量取舍）？
- 大模型的 Token 是如何切分（分词）的？为什么 strawberry 这类词在部分模型中会被拆成多个 Token（BPE / 子词粒度）？
- 为什么大模型以 Token 而不是字符或完整单词作为基本处理单位（与模型建模、词表、跨语言的关系）？
- 上下文窗口是不是越大越好？它对显存占用、推理延迟、调用成本和模型效果分别有什么影响（长上下文的注意力稀释、性价比权衡）？

### 工程落地与业务

- AI 辅助开发的完整工作流？真实使用场景？如何在项目中使用 AI 工具来提升效率？平时用什么 AI 工具（Cursor / Trae / Codex / 文心 / DeepSeek 等）、不同模型区别与适用场景、为什么用 Cursor？
- 能力如何封装为 Skill？触发条件？意图识别是否人工干预？多 Skill 串行 / 嵌套，依赖冲突、参数不兼容的容错设计？有无编排优先级调度？Skill 版本迭代如何平滑灰度、兼容兜底、旧版下线、故障回滚？
- 流式输出怎么实现（SSE/WebSocket、服务端 yield、前端增量渲染；处理中断 / 重连 / 超时）？用户点击停止生成后端如何立即终止？多轮对话 + 流式输出如何保证消息不乱序？跨服务流式透传？流式场景内容安全实时截断（敏感词立即停流）？
- 解决大模型输出快慢对 RAF 的优劣势、如何结合 nextTick 优化流式渲染？如何解决 Markdown 渲染抖动、增量渲染能否解决？大模型输出内容格式不对前端无法渲染怎么兜底？前端从前端角度如何防止提示词注入？
- 大模型输出的数据如何渲染成对应组件？前端如何通过接口拿到 JS 代码并在前端渲染 / 执行？AI 返回的 ECharts 图表报错了怎么办？
- 多模型路由：自动降级、流量灰度、负载均衡、故障隔离调度架构？Agent 批量任务并发（动态限流、优先级调度、故障隔离防雪崩）？Agent 上下文超长引发卡顿超时，前后端协同做裁剪、缓存复用、分片请求、增量渲染？
- AI 工作流上线后，全链路监控、异常告警、模型幻觉导致业务出错的兜底方案？Agent 推理路径有没有落库、怎么用它做质量分析和回溯？如何评估一个 Agent 系统好坏的指标体系？
- Agent 可评估性、工具调用的安全沙箱设计（模型判断 + 规则兜底双层）？Harness Engineering 怎么理解、以后会不会成为标准开发范式？
- 切换国产大模型，提示词、工具调用逻辑要改哪些点？使用什么大模型、调用成本如何管控、写一千行代码消耗多少 token / 成本多少？Token 成本问题消耗过快怎么排查（可观测、生成 LLM span 树读四类 token 用量）？
- 海量积压场景，消息队列（RabbitMQ 死信 / 延迟队列）怎么保障消费稳定性？双十一场景：大量用户询问优惠 / 退换货，如何设计 Agent 应对高并发稳定性与成本控制（意图分层、核心路径缓存、降级限流、成本实时追踪）？Agent Demo 改造成双十一级别生产系统最大架构挑战？
- AI 会替代前端吗？有了 AI 还回得去"古法编程"吗？前端如何与 AI 结合提升自己的不可替代性？AI 时代前端扮演的角色、学习路线、未来是否还有前端岗位？前端除了页面还能用来干什么？
- Coding Agent 项目是什么、为什么要做？跨轮次意图跳跃如何结合上下文判断当前意图？长时间编程任务超出上下文窗口采用什么压缩机制（参考 Claude Code）？AI 编程工具（Claude Code / Codex）架构？AI coding 辅助程度、生成代码占比？是否沉淀项目专属知识库 / 错误案例给 AI？测试 Agent 怎么实现？不同 AI 编程工具的规则 / 上下文 / 长期记忆如何共享？
- 视频理解 / 视频剪辑 Agent 从输入到成片全流程与模块？任务状态机六大状态与流转、指数退避重试、熔断、音画同步检测、字幕完整性校验？长视频转写调用什么模型、处理时长、分片上传断点续传、能否跨视频检索？
- 在前端 / Node.js 端用 ONNX Runtime 部署模型遇到过哪些问题（模型格式转换、推理 session 反复创建导致的内存泄漏），如何预防内存泄漏风险、做推理资源复用？
- 用大模型提升推荐效率（如新闻推荐）的业务思路？意图识别模块如何实现？知识库如何构建、是否用 ReAct 模式？
- 如果让你设计一个 AI Agent 工作流可视化编辑器，你觉得应该怎么设计（节点与连线的数据结构、编排与执行引擎、变量传递、调试 / 回放与异常态展示）？
- 大模型流式返回 JSON 时，前端如何做增量解析（处理未接收完整的 JSON 片段、避免半截 JSON 解析报错、错误容错与重试）？
- 前端如何和算法 / 模型团队约定流式接口、错误码与数据结构（前后端协议对齐、字段命名与版本兼容）？
- 浏览器端运行小模型（端侧推理）的价值与限制是什么（相较云端调用，在延迟 / 隐私 / 包体体积 / 能力边界上的权衡）？
- 流式聊天场景下，如何优化长文本输出的展示与交互体验（如大量增量内容渲染卡顿、滚动位置保持、长文可读性与不阻塞主线程）？

---

## 七、场景 / 手写编程题

### 异步 / 并发 / 调度

- 实现一个并发控制函数 / 异步调度器 Scheduler（控制最大并发数，结果顺序不变，任务失败不中断，收集所有结果）
- 带最大并发数限制的 Promise.all / async pool / 竞态请求只消费最后一次（p-limit）
- 实现 Promise.resolve 及手写 Promise 核心逻辑 / 手写 Promise.all
- 手写发布订阅模式（含带 once 的 EventEmitter）
- 串行执行传入的 promises（区别于 Promise.all）
- 手写 bind（可用 apply / call，不能用展开符）/ 手写 call、apply
- 手写 instanceof
- 手写类型判断 Object.prototype.toString.call
- LazyMan（链式调用调度）/ hardman 经典题 / 手写类似柯里化求积
- 手写 repeat(func, times, wait) / 实现定时器 sleep 函数
- 单例模式 / 实现一个单例 object（双检查锁）

### 手写基础算法 / 数据操作

- 手写深拷贝 / 浅拷贝（循环引用用 WeakMap；ES6 展开运算符是深拷贝还是浅拷贝）
- 手写数组原地去重 / 数组转树 / 树转扁平化（tree <-> flat）/ 嵌套对象（树）转数组
- 实现 pick / 实现 classnames 函数 / 实现 once 函数（记忆化第一次执行结果）
- 实现 myPromiseMap（数组、回调、最大并发，返回结果数组）/ 统计数组元素出现频次并筛选频次 ≥ N
- 手写：字符串中提取所有数字（含科学计数法、小数、分数、负数）/ 解析 URL 查询字符串为 JSON（含嵌套、类型转换）
- 随机生成密码（至少一个大写一个小写一个数字一个特殊符号）/ 特殊路径找嵌套对象
- 使用千位分隔符转换数字
- 反转字符串（多种办法）/ 手写反转链表（迭代 + 递归）/ 反转链表 K 个一组 / 重排链表 / 合并两个有序链表 / 原地合并两个有序数组 / 删除链表倒数第 K 个节点 / 判断链表是否有环（快慢指针）
- 无重复字符的最长子串（带 unicode 编码）/ 最长递增子序列 / 最长连续子数组和 / 最大子数组和 / 股票 II / 改版三数之和 / 三数之和（注意去重）/ 两数之和（三种解法）/ 电话号码的字母组合 / 盛水最多的容器 / 打家劫舍 II（环形房屋）/ 爬楼梯 / 跳台阶 / 硬币找零（最少硬币数）/ 复原 IP 地址 / 前 K 个高频单词 / 最小 K 个数 / 第 K 小/大元素（Quickselect）/ 多数元素 / 除自身以外数组的乘积 / 括号匹配（含有序括号、O(1)、子串提取）/ 唯一路径数 unique-paths / 二叉树右视图 / 二叉树层序遍历（含逆序）/ 对称二叉树 / 二叉树最大深度 / 二叉树最大宽度 / 二叉树路径和（回溯 + 剪枝）/ 二叉树最大路径和 / 创建二叉树求叶子节点高度 / 寻找数组中第 K 小元素 / 多个已排序整数数组求交集 / 全排列 / 全排列 II / 单词搜索（剪枝）/ 最长不含重复字符子串 / 二分查找 / 二维数组查找（全局递增）/ 三角形的最短路径和 / 数组中的第 K 个最大元素 / 圆圈中最后剩下的数 / 求一组数中位数 / 实现优先级队列 / 相亲配对随机算法 / 字符串相似度 / 比较版本号 / 搜索高亮（只高亮前 N 个匹配）/ 根据成绩输出每个班级名次（可并列）/ 实现 LRU 缓存（含超时自动删除）/ 手写快排 / 合并 k 个有序链表
- 判断一个字符串是不是累加数 / 判断一个字符串是否是正确的有序括号
- 给定算术表达式字符串求值（hard）/ 迷宫题
- 实现倒计时（秒数转 时分秒 + 开始 + 复位）/ 实现深拷贝与浅拷贝（循环引用）
- 类继承：实现 车 / 汽车 / 单车 类，含成员属性、方法、默认黑色属性
- 手写：实现一个简单的 Set 类（add / remove）/ 实现深层比较 isEqual 函数
- 用 JSON.parse(JSON.stringify()) 做深拷贝有什么局限性（函数 / Symbol / 循环引用 / undefined / Date 等丢失）？
- 最长公共子串（LeetCode 1143 / 718）如何实现？
- 找出 1-N（如 1-10000）内的所有质数（埃氏筛 / 欧拉筛）？
- 求二叉树中第 N 大的数（BST 反中序遍历 / 普通二叉树 + 堆）？
- 字符串压缩：把 aabcccaaaa 压缩成 a2bc3a4（连续相同字符计数，只出现一次不加数字）？
- 给定两个数组，一个是入口文件的依赖关系、一个是参与打包的所有文件的依赖关系，输出最终打包结果（依赖图遍历 + 去重 / 循环依赖处理）？
- 找岛屿 / 岛屿数量（DFS 淹没法 与 BFS 两种写法）？
- 链表排序（对链表做归并排序，ACM 输入输出格式）？其中的 dummy 哨兵节点是必须的吗、为什么选归并而不是快排？
- 二叉树的锯齿形（Z 字形）层序遍历如何实现（奇数层正序、偶数层逆序，用双端队列或标记层级方向）？
- 二叉树的序列化与反序列化（LeetCode 297，前序/后序遍历 + 空指针标记，ACM 模式输入输出）如何实现？
- 合并区间（LeetCode 56，给定若干区间，合并所有重叠区间并输出）？

### 场景 / 业务题

- 事件循环执行顺序输出（多道代码题）/ 给出代码判断 Promise / apply / bind 的输出
- 提取 URL 中 ? 后和 # 后的参数 / 解析 URL（手撕）
- 手写 SSE 解析器、按 token 流式渲染 Markdown；流式场景下 Markdown 表格如何判断"完整解析"再一次性渲染？HTML 标签流式缓冲区处理？
- 推流场景的截流、缓冲区管理、流式数据消费 / AI 流式返回数据的打字机式传输；后端一直吐数据如何保证前端不卡（Web Worker 释放计算线程）
- 虚拟列表 / 几十万行文本渲染卡顿优化（虚拟滚动 / 分片渲染 / DocumentFragment）；表格合并单元格时懒加载设计；长列表遍历阻塞主线程用 Web Worker
- 搜索框设计 / 输入关键词推荐 / 搜索结果请求错乱（第二个先回覆盖第一个）怎么解决 / 搜索字符匹配高亮后端如何处理 / 防止用户连续点击造成多次请求
- 表单校验（含异步校验）/ 动态表单（新增动态输入项）/ 设计一个分页组件（受控还是非受控）/ 复杂表单状态管理与校验
- 如何实现表单校验（一面）/ 设计一个数据聚合优化方案 / ECharts 数据聚合实现方式
- 多租户前端改造怎么设计 / 第三方页面 iframe 集成 + 按权限控制展示 / 多系统 API 数据结构（camelCase / snake_case / 中文 key）不统一如何复用 / 20+ 页面经常改字段组件如何设计 / 拖拽式 Dashboard 系统设计
- 300 辆车实时位置 + 轨迹优化（绕圈 / 锯齿处理、GeoJSON、行政区数据裁剪）/ WebSocket 多连接（车辆位置 / 报警 / 设备状态）复用 / 图表与地图数据级联联动
- 路由配置化 JSON 结构包含哪些属性 / 对象转字符串 JSON.stringify 何时报错（循环引用）及如何判断 / 微前端自动创建子应用更自动化方式 / 用 AI 现场开发动态表单
- 前端路由系统（hash / history）实现、路由懒加载、页面过渡动画 / SPA 优化方向 / 弱网降级
- 实时同步服务端数据显示 / 修复数据更新不同步 / 实时音频通话跟大模型交互时延控制 / 现场用 websocket 完成前端实时更新排序的记分面板（含异常处理 / 心跳机制）/ 直播评论前端设计（成千上万条不卡顿）
- 高并发场景：1s 内对数据库进行 10w 次写入如何优化 / 后端高并发 1w 接口 down 了怎么处理 / 多用户并发操作同一资源竞态冲突（前端锁 / 服务锁 / 分布式锁）
- 大文件分片上传、断点续传、进度同步一致性 / 大文件上传弱网怎么办 / 文件下载断点续传（HTTP Range）
- 从零手写轻量业务状态机（核心模块与容错逻辑）/ 项目状态机六大状态与流转、指数退避重试、熔断
- 一个死锁（多线程）/ 多线程竞争状态（互斥锁）/ 数组和链表区别 / 哈希表（hashMap）原理
- 原生 HTML 实现可编辑数据表格的增删改查（新增 / 编辑 / 删除行，斑马线背景，表头点击正序倒序排序，删除弹确认框，鼠标移入显示操作列）？
- 笔试逻辑题：两个 8 斤桶 + 一个 3 斤桶，将 16 斤水平均倒入 4 个水池（各 4 斤）如何操作？
- 笔试逻辑题：两两 PK、输的人休息，小明玩 15 局、小刚 21 局、小强休息 5 局，问某一局是谁和谁 PK？
- 封装一个 v-scroll 自定义滚动指令的思路是什么？触底加载（是否到底、何时触发请求、重复触发如何拦截）的逻辑怎么实现？
- 让你封装一个弹出框（Modal）组件，说说你的封装思路（挂载方式 / 层级 / 遮罩 / 关闭时机 / API 设计）？不依赖框架用原生怎么实现？
- 手写代码：父组件下有两个子组件，子组件 A 展示数字、子组件 B 是按钮，通过父组件传递实现点击按钮改变数字（状态提升与父子通信）
- 拖拽画布 / 低代码编辑器：平台是基于 DOM、SVG 还是 Canvas 实现的、怎么做渲染选型？画布上有上百个组件、缩放到最小时视口内要展示多少组件、性能怎么保证？组件的自动吸附 / 对齐辅助线能力是怎么实现的？
- 自定义 JSON 编码协议 / 数据结构设计时，为什么用扁平数组而不是嵌套数组（树）？嵌套树结构在什么场景下会更好（可读性、增删改查效率、序列化体积、层级关系表达）？
- 手撕：实现一个支持流式追加消息的聊天列表组件（新消息到来时如何高效更新、避免整列表全量重渲染）？
- 手撕：实现一个支持多轮对话和流式返回的 API 接口（服务端如何边生成边向客户端吐数据、如何维护每轮会话上下文）？

---

## 八、HR / 软技能 / 开放题

- 自我介绍（含优势版 / 去术语版，几乎每场必问）
- 为什么选择前端 / 为什么走前端这条路 / 如何体现对前端的热爱 / 前端是怎么学习的 / 学习路径与途径？
- 平时怎么学习前端、遇到 bug 怎么解决、印象深刻的问题？最近学了哪些新技术、有无技术博客 / 开源贡献？
- 项目介绍；最有挑战 / 最有成就感的内容；你主要负责什么、遇到过哪些重难点、怎么解决的？项目是多人协作还是自己学习项目、担任什么角色？为什么做这个项目、初衷想解决什么问题？
- 实习经历介绍；实习中印象深刻的难点 / 挑战 / 收获；实习最大收获是什么；实习踩过什么坑；实习工作强度怎么样；实习大概三四个月如何自我评价？
- 职业规划？前端未来怎么发展（基础 → 工程化 / 跨端 → 架构 / 全栈）？未来发展方向是前端还是前端 + AI？对部门业务的理解与期待？
- 优缺点？最大的优势是什么？相比其他人 / 同届应届生的核心竞争力？综合评价自己（优势与不足）？
- 上一段实习为什么离职 / 为什么离开上家公司？在字节实习一年为什么又想出来找工作？已有 offer 为什么还在考虑其他机会？
- 遇到不合理的需求如何与产品讨论（argue）？团队冲突解决案例与反思？多人团队中核心技术架构职责（开发 / 设计 / 兜底 / 优化）？团队管理情景（组员能动性不强怎么解决）？
- 如何快速融入新团队并建立影响力？高强度工作下的抗压方法与时间管理？自主学习能力体现？新的业务环境下如何快速适应？
- 反问环节（业务、技术栈、培养、进度、导师制度、转正率、AI 态度等，几乎每场）
- 老家是哪里的？毕业后倾向于去哪个城市 / base 地偏好？是否为独生子女？家庭职业？恋爱状况及伴侣对工作地点的偏好？父母是否有意见？
- 留学相关（为什么留学、家人、女朋友等）/ 读研意向 / 在校成绩、基础课程（数据结构、计组、计网、数据库）？
- 是否接受某技术栈（如 Vue3）/ 能否接受纯前端 / 能否接受高强度（如 pdd 强度）？到岗时间与实习天数、能实习多久？
- 目前有其他 offer 吗、还在进行的面试流程、更倾向哪一个？既然已有 offer 为什么还在考虑其他？有在面试其他公司吗？
- 选择机会时主要考虑的因素？期望薪资（HR 常见反问）？其他行业（如国企）有机会会考虑吗？
- 为什么选择前端技术栈而不是数据或算法方向？为什么想做前端 / 为什么投某司 / 对某某业务了解？
- 兴趣爱好？还有什么其他可能流程比较靠后的公司？
- 介绍部门的业务和组织架构？前端与后端配合时处理逻辑放前端还是后台合适（指导原则）？多团队配合时怎么判定产品需求是否合理？
- 行为题：遇到解决不了的问题怎么办、最有挑战的一件事、低代码 / 跨端了解、设计模式与原则（工厂模式等）、如何培养新人 / 团队培训？
- 对 AI 的冲击与应对：AI 会替代前端吗、AI 时代学习路线、如何与老员工合作推进项目、如何保持技术敏锐度？过去一个月学了哪些 AI 知识、学习 AI 最大挑战？
- 业务理解 / 项目决策复盘类（如为什么早期用 RAG 后来又不用了、为什么没有用现成 Agent 框架而自研）？
- 你是如何参与项目流程的？从产品提需求到开发、联调、QA 测试、上线的完整流程说一下，你在各环节承担什么？
- 接到一个需求描述并不明确的任务，你该怎么做（如何澄清边界、拆解任务、与产品对齐预期）？
- 你两段实习都是前端开发，平时是后端同学配合你们吗？项目的接口规范由谁来制定、前后端如何对齐？
- 你觉得企业里的项目和你自己现在做的项目会有什么不同（技术层面 / 工作协作层面）？和产品经理、后端、测试沟通对你来说会是挑战吗？如果开始实习，第一个月最需要做哪些事？给你一个业务逻辑比较多的项目，大概需要多久能理解并快速定位到对应的业务逻辑？

---

## 九、Agent 应用开发（不限技术栈：后端 / TS / 全栈）

### Agent 服务架构与工程

- 多 Agent 平台怎么搭建（举一个实际例子，说明各子 Agent 的职责与协作方式）？
- 怎么理解 Harness（Agent 基础设施层），它与 Agent 核心推理逻辑的职责边界是什么？
- 多 Agent 并发有什么性能问题，平时怎么控制 Agent 的并发量（限流 / 排队 / 异步）？
- Planner / Executor / Critic 在 Agent 中分别负责什么；Critic 是什么、为什么有时不用纯 ReAct 而要加 Critic 节点？
- 模型聚合网关（LLM Gateway）怎么设计，多个厂商大模型如何抽象（统一 schema + 适配器适配差异）？
- 模型调用失败时（按错误码分类）怎么处理，厂商限流怎么处理（冷却 / 排队 / 退避）？
- LLM 调用的 token 成本怎么统计（厂商返回消耗 / 不返回时如何用数学模型与分词器估算）？
- 长时记忆和短时记忆的底层实现方式（基于 Redis / 数据库分别怎么存）？
- 知识库 / RAG 项目试过存放多少文档，容量与规模怎么评估？
- RAG 全站数据定期同步怎么做（checksum 增量更新、定长 + overlap 分块、结构化文档分块、TopK + Rerank、轻量血缘）？
- Agent 项目技术选型：用 LangGraph 等框架还是完全自研、Master+Sub Agent 还是 Workflow，各自的适用边界与选型依据（何时该上框架、何时该自研）？
- 首次生成链路与多轮补充（增量生成）链路的路由如何区分与实现（何时走全新生成、何时走补充链路）？
- 多个子 Agent 给出冲突结论时如何仲裁（投票 / 置信度 / 规则裁决）？什么情况下需要人工介入兜底（高风险、低置信、合规边界）？
- Agent 多工具调用时如何做输出校验，防止工具返回无效 / 不合规结果（schema 校验 / 结果断言 / 失败重试或降级）？
- RAG 场景下向量数据库如何做分片，海量文档下如何优化召回速度与吞吐（分片策略 / 索引类型 / 并行查询）？
- LangChain 与 LlamaIndex 各自适合什么业务场景（何时用编排优先的 Chain 式框架、何时用索引 / 检索优先框架）？
- 并发场景下 Agent 会话（Session）如何设计（多用户并发、会话隔离、状态一致性、上下文隔离）？
- Skill 的按需加载（动态加载）是怎么实现的（如何减少启动期加载所有 Skill 的开销、按需注入上下文）？
- CoT（思维链）对 Agent 执行效果带来哪些提升，又有什么局限性（推理质量 vs token 消耗 / 延迟 / 过度思考）？
- SFT 数据集构建有哪些要点（数据质量 / 多样性 / 指令格式 / 难例挖掘 / 配比）？
- 写好一个 Harness（Agent 运行时环境）必须具备哪些核心能力（工具注册与管理、上下文管理、执行控制：超时 / 最大步数 / 循环检测 / 并发控制、可观测性 Tracing、沙箱隔离）？
- 多 Agent 之间如何通信？同步（直接调用 / RPC / HTTP）与异步（消息队列 / 事件总线 / 共享存储）各自的适用场景与要解决的核心问题（协议一致性 / 超时 / 幂等性）？
- 后端（如 Node.js）服务如何与 Python 模型服务通信（协议选型：HTTP / gRPC / 消息队列，各自的适用场景与优劣）？
- 从零设计一套高并发 AI 对话系统，你会怎么拆分服务（网关 / 对话服务 / 模型代理 / 召回服务等模块边界划分与职责）？
- 大体积输入（如单次上千条 / 10–20MB 日志）如何裁剪、去重、压缩后喂给 LLM？如何保留完整日志、让模型先定方向再按需 grep 关键信息（按日志级别 / 关键词屏蔽、重复合并、摘要压缩）？
- 一个 Skill 中哪些是固定部分（元数据 / IO 契约 / 基础提示词 / 工具调用地址）、哪些是随观察动态调整的（实例化参数 / 动态指令 / 条件流程），Skill 如何自演进？
- 前端 / Node 侧 Agent 的多层会话存储如何设计（内存态保证流式连续渲染 → 浏览器缓存覆盖刷新 / 重开 → 落盘跨端拉历史），如 zustand + IndexedDB + SessionStorage + SQLite 的分层？
- 如何从对话 session 中沉淀真正有价值的长期记忆 / 知识、过滤"改字号 / 美化"这类操作噪声（规则闸门 + 信号词 + remember 工具 / 保留清单丢弃清单 + 增量摘要）？

### Agent 应用部署与可观测

- 了解哪些进程 / 内存监控工具（如 top / htop / pmap / /proc）？线上服务出现 CPU 飙高或内存持续增长（疑似泄漏）时如何排查（火焰图 / heap dump / 逐步缩小范围）？
- mmap 是什么？它解决了什么问题，适合哪些文件映射或进程间通信场景（大文件读写、共享内存、零拷贝）？
- writev 是什么？它如何通过聚合多个缓冲区减少系统调用开销（分散写、减少上下文切换）？

### Agent 评测与质量保障

- Agent 任务成功率这类测试数据怎么测的（用什么指标、如何构造评测集）？
- 没有用户反馈时如何对 Agent 效果做有效抽检（采样 / 离线评测 / 自动评分）？
- 出现 Badcase 时如何快速定位是哪个环节、哪个 Agent 出的问题？如何判断该对哪个 Agent 做 SFT 优化？
- Prompt 调优出现「修好一类、坏了另一类」的回归问题怎么解决？
- Prompt 调到极限后，优先换模型、调参数还是上 SFT？各自的判断依据是什么？
- 代码单测的分支覆盖率怎么统计、代码插桩（instrumentation）的原理是什么？
- Agent 工具调用的成功率如何定义与度量（成功 / 失败 / 超时 / 脏数据口径）？项目中最初的数据是多少、后续通过哪些手段提升？
- Prompt 模板如何管理、测试和灰度发布（作为版本化制品，如何验证改动效果、平滑上线、出问题时回滚）？
- RAG / 知识库案例库防污染：如何拦截"假案例"（如临时规避根因却误以为已解决），事前严控入库 / 事中全量清洗 / 事后持续治理分别怎么做（结构化模板、置信度、语义冲突检测、版本标签）？
- 不靠大量人力 review，如何自动化保障知识库入库质量（LLM 入库预检 + 相似度去重合并 + 上线后采纳率 / 解决率反馈闭环自动下线低效案例）？

### Agent 产品与业务落地

- 如何设计一个订单客服 Agent；转人工这一块怎么处理；转人工意图自动识别怎么做；Agent 意图识别怎么调优？
- 如何进行意图识别（Agent 路由 / 分发逻辑，根据用户输入选择对应 Agent 或工具）？
- 并行化意图识别是什么、为什么要做、如何实现（与普通串行意图识别的区别、适用场景）？
- 场景题：用 Agent 实现广告投放的自我迭代（自动判别哪种广告投放方式最优并执行），你怎么设计？
- 知识库 / RAG 能否解析语音与视频，图片是怎么存储的，Excel 表格是怎么切分的（多模态 / 结构化文档解析）？
- 当前 Agent 技术的发展阶段与现状你怎么看，现阶段适合落地在哪些业务场景（客服 / 代码 / 数据分析 / 流程自动化）？
- 设计一个"告警 → 根因定位 → 自动修复"的运维 / AIOps Agent 端到端闭环（告警接入 → 查知识库定方向 → 工具调用 → ReAct 收敛 → HITL 人工复核 → 一键执行修复）？

## 参考来源 / 延伸阅读

- 牛客网（笔面经 / 题库主体来源）：[nowcoder.com](https://www.nowcoder.com/)
- LeetCode 力扣（算法题配套练习）：[leetcode.cn](https://leetcode.cn/)
- CodeTop（按公司真题筛选）：[codetop.cc](https://codetop.cc/)
- 各厂面经讨论区（牛客讨论区按公司检索最新真题）
- Agent 相比传统工作流（Workflow）的优势是什么，生产环境落地会遇到哪些工程问题（稳定性 / 成本 / 可观测 / 评测闭环）？
